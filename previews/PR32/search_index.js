var documenterSearchIndex = {"docs":
[{"category":"section","location":"references/#References","page":"References","text":"Works this package builds on. Every DOI is verified upstream by the VerifyReferences CI, so a fabricated or dangling citation cannot reach main.\n\nR. L. Graham. Bounds on Multiprocessing Timing Anomalies. SIAM Journal on Applied Mathematics 17, 416–429 (1969).\n\n\n\n","title":"References"},{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"module","location":"api/#TestShards.TestShards","page":"API","text":"TestShards\n\nSplit a Julia test suite across CI jobs, and record what each piece did.\n\nThe shardable units are whatever runtests.jl includes — not files matching a naming convention, and not a list you maintain. @shard shadows include inside its block, so every include call is observed at the moment it happens; a unit computed by a loop over readdir is seen exactly like a literal one.\n\nusing MyPackage, TestShards\n\nTestShards.@shard begin\n    include(\"core/test_a.jl\")\n    for f in readdir(\"solver\"; join = true)\n        include(f)                       # computed includes are units too\n    end\n    TestShards.@unit \"stateful\" begin    # one unit: same shard, in this order\n        include(\"stateful/01_setup.jl\")\n        include(\"stateful/02_use.jl\")\n    end\nend\n\nTwo properties make this safe, and both come from every shard observing the whole sequence and skipping what is not its own:\n\nNothing is silently dropped. A unit that no shard claims cannot exist — assignment is a total function of the observed sequence, computed identically in every shard.\nIdentity is shard-independent. A unit is (key, index) where index is its position in the full sequence, so records from different shards merge into one ordered report.\n\nWithin a shard, units run in observed order. Across shards order is not preserved — that is what parallelism means — so anything order-dependent belongs in one @unit.\n\nEach unit's @testset tree is captured with its per-testset timings and outcomes, giving a file → testset hierarchy that a reporting layer can render directly (one page per unit, one section per testset). Attach evidence to the running testset with evidence!.\n\nSharding never changes what a bare Pkg.test() means: with no environment set, everything runs.\n\n\n\n\n\n","title":"TestShards.TestShards"},{"category":"type","location":"api/#TestShards.Assigned","page":"API","text":"Assigned <: Ownership\n\nOwnership by computation: every shard packs the same timing history the same way, so a unit belongs to exactly one shard and no shard has to ask anyone.\n\nThat determinism is the package's central guarantee — and, under a congested queue, precisely what is wrong: a shard that starts five minutes late still owns its share, and everyone else waits for it. Determinism is what prevents a late runner from taking less.\n\n\n\n\n\n","title":"TestShards.Assigned"},{"category":"type","location":"api/#TestShards.Bottleneck","page":"API","text":"Bottleneck\n\nWhat is setting this run's wall clock. One of QueueBound, FixedCostBound, FloorBound or WorkBound.\n\nThe same numbers mean opposite things at different scales, and that is the whole reason this is a type rather than a sentence in the manual. A 150s suite with a 49s per-shard cost and a 130s start window is being destroyed by both; a 40-minute suite with the identical figures is barely inconvenienced. A reader can work that out from the raw numbers — but then every consumer repository has to work it out again, and the ones that get it wrong get it wrong silently. So bottleneck decides, and remedy and usable_shards dispatch on the answer.\n\n\n\n\n\n","title":"TestShards.Bottleneck"},{"category":"type","location":"api/#TestShards.Claimed","page":"API","text":"Claimed <: Ownership\n\nOwnership by claim: a shard sweeps the whole observed sequence and runs whatever nobody has taken yet. A shard that starts late claims what is left; if nothing is left it exits, having wasted only its own startup instead of delaying everyone.\n\nThe primitive is creating a git ref, which is an atomic compare-and-swap on the server: POST /repos/:repo/git/refs creates it or returns 422 because someone else already did. No external service and no new secret — contents: write is already granted for the timing history.\n\ngit push was the obvious implementation and is the wrong one: every shard would push the same commit, so a push to an existing ref pointing at that same commit is a no-op that SUCCEEDS, and every shard would believe it had won. Creating a ref fails whatever the sha is.\n\nIt buys work stealing at the price of one round trip per unit, and it introduces a failure mode static assignment does not have: a shard that claims a unit and then dies leaves a hole. That is why completeness is not optional — a run that silently skips a unit is exactly the green-but-wrong outcome this package refuses everywhere else.\n\nmin_seconds keeps cheap units on Assigned: below it, a round trip costs more than the unit does. Mixing is safe — static assignment is total over the units it covers, so every unit is still owned exactly once.\n\n\n\n\n\n","title":"TestShards.Claimed"},{"category":"type","location":"api/#TestShards.Completeness","page":"API","text":"Completeness\n\nWhether the shards between them ran every unit they observed, exactly once.\n\nUnder Assigned this is a theorem: assignment is a total function of the observed sequence, so a unit no shard claims cannot exist. Under Claimed it is not — a shard that claims a unit and then dies or is cancelled leaves the unit claimed and never run, and the merged records are short by one with nothing to say so. That is the green-but-wrong outcome this package refuses everywhere else, which is why claiming does not ship without this check.\n\nIt is worth running under both. It costs nothing, and it turns the guarantee from something the design argues into something each run demonstrates.\n\n\n\n\n\n","title":"TestShards.Completeness"},{"category":"type","location":"api/#TestShards.Diagnosis","page":"API","text":"Diagnosis\n\nWhat the timing history says about the shape of the suite, rather than about any one run.\n\nwalls is the predicted wall clock per shard count under the model wall(N) = fixed + max_bin(N): a shard pays a fixed cost (checkout, depot restore, precompile) that does not shrink when you add shards, plus the load of its heaviest bin. knee is the smallest N at which walls stops improving — past it, more shards buy nothing and cost a fixed price each.\n\nThe floor is the single heaviest unit: no split of the suite across jobs can finish sooner than that, so floor_unit is the only place where more parallelism can come from.\n\nobserved is the same run as measured rather than modelled, when the shards reported their windows. The model's whole premise is that the shards overlap; observed is what says whether they did.\n\n\n\n\n\n","title":"TestShards.Diagnosis"},{"category":"type","location":"api/#TestShards.FixedCostBound","page":"API","text":"FixedCostBound <: Bottleneck\n\nA shard spends longer getting ready than testing. Splitting further multiplies the setup and buys almost nothing; the cost itself has to come down.\n\n\n\n\n\n","title":"TestShards.FixedCostBound"},{"category":"type","location":"api/#TestShards.FloorBound","page":"API","text":"FloorBound <: Bottleneck\n\nThe heaviest single unit is what is left. No split across jobs beats it, so it has to be cut in two — split_here names where.\n\n\n\n\n\n","title":"TestShards.FloorBound"},{"category":"type","location":"api/#TestShards.LcovFile","page":"API","text":"LcovFile\n\nOne source file's coverage, merged across the shards that reported it.\n\nbranches maps (line, block, branch) to the number of times it was taken, or nothing for lcov's -: reached by no shard at all.\n\n\n\n\n\n","title":"TestShards.LcovFile"},{"category":"type","location":"api/#TestShards.Observation","page":"API","text":"Observation\n\nWhat the shards of one run actually did in absolute time, as opposed to what the model predicts.\n\nThe model behind Diagnosis assumes the shards run concurrently. effective is the number of shards that assumption was worth: the work done divided by the wall clock it took. It equals the shard count only when they truly overlap, and falls towards 1 — or below it, since each shard re-pays first-use compilation — as the queue spreads them out.\n\n\n\n\n\n","title":"TestShards.Observation"},{"category":"type","location":"api/#TestShards.Ownership","page":"API","text":"Ownership\n\nHow a shard decides whether a unit is its to run: Assigned or Claimed.\n\nThe two are answers to different questions, and which one is right depends on something no suite can know about itself — whether the runners start together. Observation measures that, and QueueBound is the diagnosis that says Claimed is worth its cost here.\n\n\n\n\n\n","title":"TestShards.Ownership"},{"category":"type","location":"api/#TestShards.QueueBound","page":"API","text":"QueueBound <: Bottleneck\n\nThe shards did not run at the same time, so the wall clock is set by the last one to start. Nothing about the split can fix this — see Observation.\n\n\n\n\n\n","title":"TestShards.QueueBound"},{"category":"type","location":"api/#TestShards.Section","page":"API","text":"Section\n\nOne @testset, with its nested sections. duration is wall-clock from Julia's own testset bookkeeping, so it is per-section rather than per-file.\n\n\n\n\n\n","title":"TestShards.Section"},{"category":"type","location":"api/#TestShards.ShardWindow","page":"API","text":"ShardWindow\n\nWhen one shard ran, in absolute time, and how much of that window it spent on units.\n\nThe per-unit durations say how the work divides; they cannot say whether the shards ran at the same time. Under a congested queue they do not, and then the wall clock is set by the last shard to start rather than by the heaviest bin. This is the record that makes that visible: merged across shards it gives the start window, the observed wall clock and, by subtraction, the fixed cost each shard actually paid.\n\nstarted and finished are epoch seconds from the runner's own clock, so a spread of a second or two between shards is noise rather than a queue effect.\n\nThe window runs from the job's start — CI reports it through TESTSHARDS_JOB_START — to the moment the test process ends, which is as far as a shard can see: it is not running when its job finishes. Work the job does after the tests — processing coverage, uploading artefacts — is therefore outside it, and measured here that tail is about half the fixed cost on this suite.\n\nCI closes the gap from outside by stamping the job's end into an ended-*.tsv that load_shards folds back in. Without that file finished is the process end and fixed_cost is a lower bound.\n\n\n\n\n\n","title":"TestShards.ShardWindow"},{"category":"type","location":"api/#TestShards.UnitRecord","page":"API","text":"UnitRecord\n\nOne shardable unit: what ran, where, for how long, and what it established.\n\nindex is the unit's position in the FULL observed sequence, identical in every shard, so records merged from separate jobs sort back into source order.\n\n\n\n\n\n","title":"TestShards.UnitRecord"},{"category":"type","location":"api/#TestShards.WorkBound","page":"API","text":"WorkBound <: Bottleneck\n\nNothing is in the way: the work still divides, and more shards would still make the run finish sooner. This is the regime the whole design assumes, and the only one in which raising the shard count is the right move.\n\n\n\n\n\n","title":"TestShards.WorkBound"},{"category":"method","location":"api/#TestShards._claim-Tuple{TestShards.Claimed, Integer}","page":"API","text":"_claim(c::Claimed, index) -> Bool\n\nTry to become the owner of index. true iff this process created the ref.\n\n201 is a win and 422 is a loss — both are answers. Anything else is the network or the token, which is NOT an answer: it is retried, and if it never answers the shard errors rather than guessing. Guessing \"no\" silently drops a unit; guessing \"yes\" runs it twice.\n\n\n\n\n\n","title":"TestShards._claim"},{"category":"method","location":"api/#TestShards._max_bin-Tuple{AbstractDict, Integer}","page":"API","text":"Load of the heaviest bin when LPT-packing timings into n shards.\n\n\n\n\n\n","title":"TestShards._max_bin"},{"category":"method","location":"api/#TestShards._max_bin_at-Tuple{TestShards.Diagnosis, Integer}","page":"API","text":"The heaviest bin at n, recovered from the wall curve so the timings need not be kept.\n\n\n\n\n\n","title":"TestShards._max_bin_at"},{"category":"method","location":"api/#TestShards._ownership-Tuple{Any}","page":"API","text":"_ownership(env) -> Ownership\n\nClaimed when TESTSHARDS_CLAIM is set AND everything it needs is there, otherwise Assigned.\n\nAsking for claiming without the means is an ERROR, not a downgrade. Falling back quietly would turn \"the token was not passed\" into \"the shards ran what they were assigned\", which is a working run with the wrong strategy — and the whole point of the input is to compare the two.\n\n\n\n\n\n","title":"TestShards._ownership"},{"category":"method","location":"api/#TestShards._owns-Tuple{TestShards.Assigned, TestShards.ShardContext, AbstractString, Integer}","page":"API","text":"The historical assignment, and round-robin over what the history has not seen.\n\n\n\n\n\n","title":"TestShards._owns"},{"category":"method","location":"api/#TestShards._owns-Tuple{TestShards.Claimed, TestShards.ShardContext, AbstractString, Integer}","page":"API","text":"Take it if nobody else has. Cheap units are left to Assigned: below the threshold a round trip costs more than running the unit would, and mixing is safe because static assignment is total over the units it still covers.\n\n\n\n\n\n","title":"TestShards._owns"},{"category":"method","location":"api/#TestShards._owns-Tuple{TestShards.ShardContext, AbstractString, Integer}","page":"API","text":"_owns(ctx, key) -> Bool\n\nDoes this shard run key? Manual mode consults the explicit list. Auto mode consults the history-derived assignment, and falls back to round-robin over the unknown units in observation order — so a test file added since the last recorded run is still guaranteed to land in exactly one shard.\n\n\n\n\n\n","title":"TestShards._owns"},{"category":"method","location":"api/#TestShards._plain_include-Tuple{Any}","page":"API","text":"Include a file as part of the current unit, without opening a new one.\n\n\n\n\n\n","title":"TestShards._plain_include"},{"category":"method","location":"api/#TestShards._run-Tuple{TestShards.ShardContext, AbstractString, Any}","page":"API","text":"_run(ctx, key, body)\n\nObserve a unit, and run body if this shard owns it. The observation counter advances either way — that is what keeps index, and the round-robin fallback, identical across shards.\n\nThe testset is pushed and popped by hand rather than via @testset so that the tree can be read back even when the unit failed: a top-level @testset throws before returning its result. Failure is re-signalled once, at the end of the whole block.\n\n\n\n\n\n","title":"TestShards._run"},{"category":"method","location":"api/#TestShards.assign-Tuple{AbstractDict, Integer}","page":"API","text":"assign(timings, n) -> Dict{String,String}\n\nLongest-processing-time bin packing of the KNOWN units: heaviest first into the least-loaded shard. Computed identically in every shard from the same history, so no coordination is needed and no unit can land in two shards.\n\nUnits absent from the history are not here; they are assigned on sight, round-robin over the order they are observed in (see _owns), which is also identical everywhere.\n\n\n\n\n\n","title":"TestShards.assign"},{"category":"method","location":"api/#TestShards.bottleneck-Tuple{TestShards.Diagnosis}","page":"API","text":"bottleneck(d::Diagnosis) -> Bottleneck\n\nWhich of the four regimes this suite is in.\n\nThey are tested in the order below, because that is the order in which fixing one exposes the next. A queue-bound run tells you nothing about its balance — the balance was never given a chance to matter — so there is no point reporting the floor at it.\n\nQueueBound — the shards spent a large share of the run waiting for the last one to START. Needs shards to detect; without windows a run cannot know this happened to it.\nFixedCostBound — the per-shard fixed cost exceeds the heaviest bin, i.e. a shard spends more of its life getting ready than testing.\nFloorBound — the requested shard count is at or past the knee, so the heaviest single unit is what remains.\nWorkBound — otherwise.\n\nThe queue test is measured, not modelled, and it used to be the other way round: \"the observed wall clock is well above the predicted one\". That reads as a queue problem and is not one. critical_path is built on fixed, and fixed is a lower bound — the shard windows close when the test process exits, so per-shard work after the tests is outside them (see ShardWindow). An understated fixed understates the prediction, and the gap that opens gets blamed on the queue. Caught on this package's own CI: eight shards started 2s apart and were still called QueueBound, on a 16.7s gap the start window could account for at most 2s of.\n\nSo the question is asked directly. If the last shard started 2s after the first, the queue did not set a 70s wall clock, whatever the model expected.\n\n\n\n\n\n","title":"TestShards.bottleneck"},{"category":"method","location":"api/#TestShards.completeness-Tuple{AbstractVector{TestShards.ShardWindow}, AbstractVector{<:Tuple{Int64, String, String}}}","page":"API","text":"completeness(windows, ran) -> Completeness\n\nCheck the run against itself: every shard reports how many units it OBSERVED, and each reports which positions it took. The two must reconcile.\n\n\n\n\n\n","title":"TestShards.completeness"},{"category":"function","location":"api/#TestShards.completeness_cli","page":"API","text":"completeness_cli(args = ARGS) -> Int\n\nshards.tsv ran.tsv — 0 if the run covered its suite, 1 if it did not. The CI gate calls this, which is the only reason claiming is safe to offer at all.\n\n\n\n\n\n","title":"TestShards.completeness_cli"},{"category":"method","location":"api/#TestShards.completeness_report-Tuple{TestShards.Completeness}","page":"API","text":"completeness_report(c) -> String\n\nThe check as Markdown, naming what is missing rather than only that something is.\n\n\n\n\n\n","title":"TestShards.completeness_report"},{"category":"method","location":"api/#TestShards.current-Tuple{}","page":"API","text":"current() -> Union{Nothing,ShardContext}\n\nThe @shard block currently executing, or nothing outside one.\n\n\n\n\n\n","title":"TestShards.current"},{"category":"method","location":"api/#TestShards.diagnose-Tuple{AbstractDict}","page":"API","text":"diagnose(timings; n = 8, fixed = 0.0, sections = Dict(), shards = ShardWindow[]) -> Diagnosis\n\nAnswer three questions the raw numbers do not: how many shards this suite can actually use, what is stopping it from using more, and where to cut to move that limit.\n\nfixed is the per-shard cost that does not shrink with more shards: a shard's job wall clock minus the time its units took. It is what makes over-sharding expensive rather than merely useless. Pass shards and it is measured rather than guessed — each window carries exactly that subtraction, and their mean becomes fixed unless an explicit non-zero fixed overrides it. shards also decides whether the model's premise held: see Observation.\n\n\n\n\n\n","title":"TestShards.diagnose"},{"category":"function","location":"api/#TestShards.diagnose_cli","page":"API","text":"diagnose_cli(args = ARGS) -> Int\n\ntimings.tsv [sections.tsv [shards.tsv]] [--shards N] [--fixed SECONDS] [--ends FILE], printing the Markdown report. Used by the CI collect step so every run says where the suite is badly shaped.\n\nThe files are positional and in that order, because each one only adds detail to the answer: the timings alone say how many shards the suite can use, the sections say where to cut the unit that limits it, and the shard windows say whether the shards actually ran at the same time.\n\n\n\n\n\n","title":"TestShards.diagnose_cli"},{"category":"method","location":"api/#TestShards.diagnose_report-Tuple{TestShards.Diagnosis}","page":"API","text":"diagnose_report(d) -> String\n\nThe diagnosis as Markdown, for a CI job summary.\n\n\n\n\n\n","title":"TestShards.diagnose_report"},{"category":"method","location":"api/#TestShards.evidence!-Tuple{}","page":"API","text":"evidence!(; kwargs...)\n\nAttach evidence to the @testset currently running — what was checked, to what tolerance, on what grounds — so the report can state it without a reader going to the source:\n\n@testset \"inflation preserves the tiling\" begin\n    err = norm(inflate(t) - reference)\n    evidence!(; tolerance = 1e-12, achieved = err, oracle = \"closed-form inflation matrix\")\n    @test err < 1e-12\nend\n\nValues that are not Real, Bool, or AbstractString are stored as their string() form, so anything is safe to pass. Outside a @shard block this is a no-op, which keeps a test file runnable on its own.\n\n\n\n\n\n","title":"TestShards.evidence!"},{"category":"method","location":"api/#TestShards.fixed_cost-Tuple{TestShards.ShardWindow}","page":"API","text":"The part of a shard's window that no split of the suite can remove: checkout, depot restore, precompilation, and the sandbox Pkg.test builds before the first unit runs.\n\nExact when CI supplied the job's end (see ShardWindow and load_ends); a lower bound without it, because the window then stops at the test process rather than at the job.\n\n\n\n\n\n","title":"TestShards.fixed_cost"},{"category":"method","location":"api/#TestShards.line_totals-Tuple{TestShards.LcovFile}","page":"API","text":"(lines, hits) for one file, or summed over several.\n\n\n\n\n\n","title":"TestShards.line_totals"},{"category":"method","location":"api/#TestShards.load_ends-Tuple{AbstractString}","page":"API","text":"load_ends(path) -> Dict{String,Float64}\n\nRead merged ended-*.tsv rows, shard <TAB> epoch seconds: when each shard's JOB finished, as opposed to when its test process did.\n\nA shard cannot record this itself — it is not running any more. CI writes it after the work that follows the tests, which on this repository is coverage processing and is not small. One number per shard, so the only format knowledge outside this file is a printf.\n\n\n\n\n\n","title":"TestShards.load_ends"},{"category":"method","location":"api/#TestShards.load_ran-Tuple{AbstractString}","page":"API","text":"load_ran(path) -> Vector{Tuple{Int,String,String}}\n\nRead merged ran-*.tsv rows: position, shard, unit key. Malformed rows are skipped — but note that a row skipped here reads as a MISSING unit downstream, which fails the run rather than passing it quietly. That is the safe direction for this particular file.\n\n\n\n\n\n","title":"TestShards.load_ran"},{"category":"method","location":"api/#TestShards.load_sections-Tuple{AbstractString}","page":"API","text":"load_sections(path) -> Dict{String,Vector{Pair{String,Float64}}}\n\nRead a sections-*.tsv (unit <TAB> section path <TAB> seconds) into per-unit section lists. Malformed rows are ignored, like load_timings: a diagnosis must never be the thing that fails a run.\n\n\n\n\n\n","title":"TestShards.load_sections"},{"category":"method","location":"api/#TestShards.load_shards-Tuple{AbstractString}","page":"API","text":"load_shards(path; ends = \"\") -> Vector{ShardWindow}\n\nRead merged shard-*.tsv rows (shard, started, finished, units, unit seconds, observed). Malformed rows are ignored, like load_timings: a diagnosis must never be the thing that fails a run.\n\nends is an optional load_ends file, and it fixes a real understatement rather than adding a nicety. The finished a shard can write for itself is when its test PROCESS ended; everything the job does afterwards — processing coverage, uploading artefacts — is outside it. Measured here that tail is about half the fixed cost on this suite and about a tenth of it on a large one, so without ends fixed_cost is a lower bound, and the FixedCostBound test built on it is biased towards saying no.\n\nA shard present in ends gets the later of the two timestamps; one absent keeps its own, so a partial file degrades the measurement rather than corrupting it.\n\n\n\n\n\n","title":"TestShards.load_shards"},{"category":"method","location":"api/#TestShards.load_timings-Tuple{AbstractString}","page":"API","text":"load_timings(path) -> Dict{String,Float64}\n\nRead a \"key\\tseconds\" TSV. A missing file or a malformed row is IGNORED: the timing plane is advisory, and a truncated history must degrade the balance, never abort the run.\n\n\n\n\n\n","title":"TestShards.load_timings"},{"category":"method","location":"api/#TestShards.matrix_json-Tuple{Integer}","page":"API","text":"matrix_json(n) -> String\n\nThe GitHub Actions matrix: include: array for n shards.\n\nIt carries only labels. Which units a shard runs is decided inside the shard, from the timing history, so the planning job never loads the package under test and never needs to know what the suite contains.\n\n\n\n\n\n","title":"TestShards.matrix_json"},{"category":"method","location":"api/#TestShards.merge_lcov-Tuple{Any}","page":"API","text":"merge_lcov(paths) -> Vector{LcovFile}\n\nMerge lcov tracefiles into one record per source file, in the order the files were first seen.\n\nMerging is a UNION, and that is the whole subtlety. Every shard loads the whole package but runs only part of the suite, so each report marks only the lines that shard executed; a line missed by seven shards and hit by the eighth is covered. Concatenating the tracefiles instead leaves N records for one source file, which reads as N times the line count against a fraction of the hits: this repository's own CI reported 54.5% that way against a true 94.8%, and Codecov rejected the report outright.\n\nLine hits, function hits and branch counts are summed per key; LF/LH and the rest are recomputed by write_lcov rather than trusted from the inputs.\n\nMalformed lines are skipped rather than raising. Coverage is a byproduct of the run, so a truncated report must degrade the number, never fail the suite that produced it.\n\n\n\n\n\n","title":"TestShards.merge_lcov"},{"category":"function","location":"api/#TestShards.merge_lcov_cli","page":"API","text":"merge_lcov_cli(args = ARGS) -> Int\n\nout.info in1.info in2.info ... — merge and write, printing a Markdown summary.\n\nUsed by the CI collect step, which is why this lives in the package at all: as a step script it was unreachable from Pkg.test(), so nothing could catch it reporting 54.5% for a suite that covered 94.8%.\n\n\n\n\n\n","title":"TestShards.merge_lcov_cli"},{"category":"method","location":"api/#TestShards.observe-Tuple{AbstractVector{TestShards.ShardWindow}}","page":"API","text":"observe(windows) -> Union{Nothing,Observation}\n\nFold the shards' windows into the run-level figures. nothing for an empty list, so a caller can pass whatever CI collected without checking first.\n\n\n\n\n\n","title":"TestShards.observe"},{"category":"method","location":"api/#TestShards.peak_concurrency-Tuple{AbstractVector{TestShards.ShardWindow}}","page":"API","text":"peak_concurrency(windows) -> Int\n\nThe largest number of shards alive at the same instant.\n\nRequesting N jobs is not acquiring N runners. This counts the ones the scheduler actually granted at once, which is the ceiling on any parallelism the split could have delivered.\n\n\n\n\n\n","title":"TestShards.peak_concurrency"},{"category":"method","location":"api/#TestShards.remedy-Tuple{TestShards.Diagnosis}","page":"API","text":"remedy(d::Diagnosis) -> String\n\nWhat to do about it, in one sentence, chosen by dispatch on bottleneck rather than left to the reader.\n\n\n\n\n\n","title":"TestShards.remedy"},{"category":"method","location":"api/#TestShards.usable_shards-Tuple{TestShards.Diagnosis}","page":"API","text":"usable_shards(d::Diagnosis) -> Int\n\nHow many shards are worth starting, which is not always the knee.\n\nUnder QueueBound it is the number of runners the scheduler actually granted at once: requesting more produced jobs that queued rather than parallelism. Otherwise it is the knee.\n\n\n\n\n\n","title":"TestShards.usable_shards"},{"category":"method","location":"api/#TestShards.window-Tuple{TestShards.ShardWindow}","page":"API","text":"How long this shard's job was alive.\n\n\n\n\n\n","title":"TestShards.window"},{"category":"method","location":"api/#TestShards.write_lcov-Tuple{AbstractString, AbstractVector{TestShards.LcovFile}}","page":"API","text":"write_lcov(path, files)\n\nWrite merged LcovFiles back out as a tracefile, recomputing every summary line (LF/LH, FNF/FNH, BRF/BRH) from the merged records so they cannot disagree with them.\n\n\n\n\n\n","title":"TestShards.write_lcov"},{"category":"method","location":"api/#TestShards.write_records-Tuple{TestShards.ShardContext, AbstractString}","page":"API","text":"write_records(ctx, dir)\n\nWrite this shard's records as JSONL (one unit per line) plus its timings as TSV.\n\nTwo files because they have different consumers and different lifetimes: the TSV is the planner's history and is merged into a single ledger, while the JSONL is the report's input and is merged into one ordered document. Both key on the same unit key, so they always agree.\n\n\n\n\n\n","title":"TestShards.write_records"},{"category":"macro","location":"api/#TestShards.@shard-Tuple{Any}","page":"API","text":"@shard begin ... end\n\nRun the block with include shadowed, so each include is a shardable unit. See the module docstring for the whole picture.\n\nThe test root — what unit keys are relative to — is the directory of the file this macro is written in, so keys are stable no matter where CI runs from.\n\n\n\n\n\n","title":"TestShards.@shard"},{"category":"macro","location":"api/#TestShards.@unit-Tuple{Any, Any}","page":"API","text":"@unit \"name\" begin ... end\n\nTreat everything in the block as ONE shardable unit: it runs in a single shard, in the order written. This is the only way to keep order-dependent files together, since across shards order is not preserved.\n\nIt is also the manual-mode handle: in manual mode a shard is given unit names to run, so naming a unit is how you assign work by hand rather than by measured time.\n\n\n\n\n\n","title":"TestShards.@unit"},{"category":"section","location":"getting-started/#Getting-started","page":"Getting started","text":"","title":"Getting started"},{"category":"section","location":"getting-started/#Install","page":"Getting started","text":"pkg> add TestShards\n\nTestShards is used from your test suite, so it belongs in your test dependencies:\n\n[extras]\nTestShards = \"acceef1d-f5e0-4fe4-a546-818dc56ce7b2\"\n\n[targets]\ntest = [\"Test\", \"TestShards\"]","title":"Install"},{"category":"section","location":"getting-started/#Wrap-your-suite","page":"Getting started","text":"Take whatever test/runtests.jl already does and put it inside @shard:\n\nusing MyPackage, TestShards\n\nTestShards.@shard begin\n    include(\"core/a.jl\")\n    include(\"core/b.jl\")\n    for f in readdir(joinpath(@__DIR__, \"solver\"); join = true)\n        include(f)\n    end\nend\n\nEach include becomes one shardable unit. Nothing else changes: run Pkg.test() and the whole suite runs, in order, as before.","title":"Wrap your suite"},{"category":"section","location":"getting-started/#Wire-up-CI","page":"Getting started","text":"name: CI\non:\n  push:\n    branches: [main]\n  pull_request:\n\njobs:\n  test:\n    permissions:\n      contents: write        # the timing history is pushed to a `ci-timings` branch\n    uses: QAtlasHub/TestShards.jl/.github/workflows/sharded-tests.yml@main\n    with:\n      shards: 8\n    secrets: inherit\n\nwarning: The `contents: write` grant is required\nA called workflow's jobs may not request more permission than the caller granted, and recording the timing history needs write access. Granting less makes the run fail to start, with no log to read.","title":"Wire up CI"},{"category":"section","location":"getting-started/#Inputs","page":"Getting started","text":"input default \nshards 8 how many jobs to split across\nrunner '\"ubuntu-latest\"' runs-on as JSON, e.g. '[\"self-hosted\",\"rosina\"]'\njulia-version '1' \ntest-root 'test' directory holding runtests.jl\ncoverage true merge the shards' coverage and upload it\nsteal false claim work instead of being assigned it — see Guarantees\nsteal-min-seconds 0 with steal, leave units cheaper than this statically assigned\ndiagnose true report what the history says about the suite's shape\nfixed-cost-seconds '0' per-shard overhead, for the cost figures (see Balancing)\n\nThe workflow exposes an All shards passed job — that is the one to require in branch protection, rather than the individual shards, whose names change with shards.","title":"Inputs"},{"category":"section","location":"getting-started/#Run-one-shard-locally","page":"Getting started","text":"Sharding is driven entirely by environment variables, so you can reproduce any CI shard:\n\nTESTSHARDS_ID=s3 TESTSHARDS_N=8 julia --project -e 'using Pkg; Pkg.test()'\n\nOr run an explicit selection, which is usually what you want when chasing one failure:\n\nTESTSHARDS_UNITS=\"core/a.jl,solver/heavy.jl\" julia --project -e 'using Pkg; Pkg.test()'\n\nvariable meaning\nTESTSHARDS_ID this shard's label\nTESTSHARDS_N number of shards\nTESTSHARDS_UNITS explicit unit list; takes precedence over the above\nTESTSHARDS_TIMINGS timing history TSV used to balance\nTESTSHARDS_OUT where to write records and timings\n\nWith none of them set, everything runs.","title":"Run one shard locally"},{"category":"section","location":"guarantees/#Guarantees","page":"Guarantees","text":"Sharding fails in ways that leave CI green, so the interesting question is not what works but what cannot silently go wrong.","title":"Guarantees"},{"category":"section","location":"guarantees/#The-split-is-a-partition","page":"Guarantees","text":"Every shard observes the whole sequence of units and skips what is not its own. Assignment is therefore a total, deterministic function of that sequence, computed identically everywhere:\n\na unit runs in exactly one shard — never twice, never nowhere;\na unit no shard claims cannot exist;\na unit with no timing history is still assigned, so adding a test file cannot leave it unrun.","title":"The split is a partition"},{"category":"section","location":"guarantees/#…and-every-run-proves-it","page":"Guarantees","text":"The paragraph above is an argument. Each run also checks it: every shard reports how many units it observed and which positions it took, and the collect job reconciles them with TestShards.completeness. The run fails if a unit ran nowhere, if a unit ran twice, or if two shards disagreed about how many units the suite has.\n\nUnder the default Assigned ownership that check can only pass — which is the point of running it anyway, at no cost. Under Claimed it is load-bearing: see below.","title":"…and every run proves it"},{"category":"section","location":"guarantees/#Work-stealing,-and-the-guarantee-it-gives-up","page":"Guarantees","text":"steal: true replaces assignment with a claim — a shard runs whatever nobody has taken, so one that starts late takes less work instead of delaying everyone. It is worth it exactly when the diagnosis says QueueBound.","title":"Work stealing, and the guarantee it gives up"},{"category":"section","location":"guarantees/#How-much-it-is-worth,-and-when-it-is-not","page":"Guarantees","text":"Under assignment the run cannot finish before the last shard to start has also done its share:\n\nwall ≈ last_start + fixed + serial/N\n\nUnder claiming that shard finds nothing left and does none of it:\n\nwall ≈ max(when the early shards absorb everything, last_start + fixed)\n\nSo what stealing removes from the critical path is one share, serial/N — and no more, however many shards arrived late, because the wall clock is set by whichever finishes last either way.\n\nWhether that is worth having is decided by one ratio, share / fixed, and the diagnosis already computes it: FixedCostBound is exactly the case where the fixed cost exceeds the heaviest bin, which is to say share < fixed.\n\nWork-dominated (share > fixed) — a late shard's share is the largest single item on the critical path, and stealing removes it. A shard that claims nothing costs fixed instead of fixed + share, so it finishes well before the working ones and never sets the wall clock.\nSetup-dominated (FixedCostBound) — the shards that claim nothing still pay for checkout, Julia, the depot and coverage, and that is more than the work stealing saved. They can finish last while having run nothing at all.\n\nMeasured, two suites in the same organisation:\n\n serial/N fixed ratio\nthis package, N=8 27 s 49 s 0.55 — setup-dominated\nQAtlas.jl, N=16 255–651 s 225–271 s 1.1–2.6 — work-dominated\n\nNote what that table does not say. It is tempting to conclude that a larger shard count makes stealing worth less, since serial/N shrinks as N grows — but QAtlas runs twice the shard count and is still work-dominated by a wide margin. N only matters through the share it produces; what decides the question is that share against the fixed cost, and a big suite split sixteen ways still has more work per shard than a small one split eight ways.\n\nSo: steal when a run is QueueBound and the suite is not FixedCostBound. This package's own suite is the second kind, which is why stealing does not help it — and why measuring it here says nothing about whether it helps yours.\n\nOne measurement to trust more than the others: a shard that claims nothing spends its whole life on setup, so its wall clock is the fixed cost, with nothing to subtract. In a run here with a 119 s start window, four shards ran zero units and reported 46–75 s, against the 24.7 s the in-process windows report. That is the understatement of #16, confirmed from the other side — and it is worst on small suites, where the trailing coverage step the window misses is half the fixed cost rather than a tenth of it.\n\nIt also gives up the theorem. A shard that claims a unit and is then cancelled, OOM-killed or disconnected leaves that unit claimed and never run, and the merged records are simply short by one. That is why the completeness check is not optional and not conditional — with claiming on, it is the only thing between a dead runner and a green run that never tested part of the suite.\n\nThree further things stay loud rather than convenient:\n\na shard asked to claim without the token, repository or sha to do it errors; falling back to assignment would run the suite with the strategy you did not ask for and report success;\na claim request that neither succeeds nor is refused — a network failure — errors after retrying, because both available guesses are wrong: one drops a unit, the other runs it twice;\nthe claim namespace includes the run id and attempt, so a re-run cannot inherit the previous attempt's claims and skip everything.","title":"How much it is worth, and when it is not"},{"category":"section","location":"guarantees/#What-is-an-error-rather-than-a-warning","page":"Guarantees","text":"situation why it must be loud\na shard label with no selection every shard would run the whole suite: green, slow, and meaningless\na unit named that the suite does not have the plan and the suite disagree about what exists\na green default-branch run that produced no timings balancing would silently fall back to an even split forever","title":"What is an error rather than a warning"},{"category":"section","location":"guarantees/#Failure-still-reports","page":"Guarantees","text":"A failing unit is recorded, does not abort its shard, and the run fails once at the end. Timings are written even for a failed run — a red unit still took however long it took, and dropping that would make the next split fall back to a stale estimate exactly where the suite is changing.","title":"Failure still reports"},{"category":"section","location":"guarantees/#The-one-rule-your-suite-must-follow","page":"Guarantees","text":"Every unit must be independently includable.\n\nA shard receives an arbitrary subset, so a file that relies on a helper defined in another file works unsharded and breaks the moment the two land in different shards. The tell, during a full run, is:\n\nWARNING: Method definition foo(Any) in module Main ... overwritten ...\n\nFix it by giving each file its own helpers, putting shared ones in a file that each includes, or binding the group into one @unit.\n\nThis is the only requirement sharding adds. Everything else about your suite stays as it was.","title":"The one rule your suite must follow"},{"category":"section","location":"units/#Units","page":"Units","text":"A unit is one thing a shard can be given. Units are discovered by watching your suite run, not by matching filenames or reading a manifest.","title":"Units"},{"category":"section","location":"units/#Every-include-is-a-unit","page":"Units","text":"@shard shadows include inside its block, so each call is observed at the moment it happens:\n\nTestShards.@shard begin\n    include(\"core/a.jl\")                 # a unit\n    for f in readdir(\"solver\"; join = true)\n        include(f)                       # each of these is a unit too\n    end\nend\n\nBecause the interception is at the call and not at the source text, a unit produced by a loop is seen exactly like a literal one. Nothing has to predict what the loop will yield.\n\nAn include inside an included file is an ordinary include — it does not create a unit. Only the calls written in the @shard block do.","title":"Every include is a unit"},{"category":"section","location":"units/#Keeping-files-together:-@unit","page":"Units","text":"Files that must run in sequence — one sets up state another reads — cannot be split across jobs. Bind them into a single unit with @unit:\n\nTestShards.@shard begin\n    include(\"core/a.jl\")\n\n    TestShards.@unit \"stateful\" begin\n        include(\"stateful/01_setup.jl\")\n        include(\"stateful/02_use.jl\")\n    end\nend\n\nThe group runs in one shard, in the order written, and the planner treats it as one heavy unit. Its name is also how you address it in manual mode.","title":"Keeping files together: @unit"},{"category":"section","location":"units/#Ordering","page":"Units","text":"Within a shard, units run in the order they were observed, regardless of how the balancer packed them.\n\nAcross shards, order is not preserved. That is what running in parallel means. If your suite is sequential end to end, it cannot be sharded — that is a property of the suite, not a limitation to work around.","title":"Ordering"},{"category":"section","location":"units/#Choosing-units-by-hand","page":"Units","text":"Setting TESTSHARDS_UNITS runs exactly the named units and nothing else, which is how a hand-declared test-group matrix works:\n\nstrategy:\n  matrix:\n    group: ['fast', 'heavy']\nenv:\n  TESTSHARDS_UNITS: ${{ matrix.group }}\n\nwith @unit \"fast\" and @unit \"heavy\" in the suite. An explicit list always wins over the automatic split.","title":"Choosing units by hand"},{"category":"section","location":"records/#Records","page":"Records","text":"Every run writes a record of what it did: one line per unit, holding that unit's @testset tree with per-testset durations and outcomes. CI merges the shards' records into one document ordered by position in the suite, so the result does not depend on how the run was split.\n\n{\"key\":\"core/a.jl\",\"index\":1,\"shard\":\"s1\",\"duration\":0.20,\n \"npass\":2,\"nfail\":0,\"nerror\":0,\"nbroken\":0,\n \"sections\":[{\"name\":\"inflation preserves the tiling\",\"duration\":0.056,\n              \"npass\":2,\"nfail\":0,\"nerror\":0,\"nbroken\":0,\n              \"evidence\":{\"tolerance\":1.0e-12,\"achieved\":3.2e-14,\n                          \"oracle\":\"closed-form inflation matrix\"},\n              \"sections\":[{\"name\":\"sub-check\",\"duration\":0.007, \"...\":\"...\"}]}]}\n\nThe shape is a unit → testset hierarchy, which maps onto a document as one page per unit and one section per testset. index is the unit's position in the full suite and is the same in every shard, so records from separate jobs sort back into source order.\n\nAlongside it, three flat TSVs:\n\nfile columns what reads it\ntimings-*.tsv unit, seconds the next run's split\nsections-*.tsv unit, section path, seconds TestShards.diagnose, to say where to split a heavy unit\nshard-*.tsv shard, started, finished, units, unit seconds TestShards.diagnose, to say whether the shards overlapped\n\nOne row per shard in the last of them, so concatenating the shards' files gives the timeline of the run — see Balancing. The timestamps are epoch seconds; CI reports the job's start through TESTSHARDS_JOB_START so that the checkout and precompilation the Julia process never sees still fall inside the window.","title":"Records"},{"category":"section","location":"records/#Coverage","page":"Records","text":"Each shard writes its own lcov.info, and the collect job merges them with TestShards.merge_lcov before the single upload.\n\nThe merge is a union, and the distinction matters more than it looks. Every shard loads the whole package but runs only part of the suite, so its report marks only the lines that shard executed — a line missed by seven shards and hit by the eighth is covered. Concatenating the tracefiles instead leaves one record per shard for the same source file, which reads as N times the line count against a fraction of the hits. This repository reported 54.5% that way for a suite that covered 94.8%, and Codecov rejected the report outright.\n\nThe merge lives in the package rather than in the workflow so that it is reachable from Pkg.test(). The version that was a step script was wrong for as long as it existed, because nothing could test it.","title":"Coverage"},{"category":"section","location":"records/#Attaching-evidence","page":"Records","text":"A passing test says only that it passed. evidence! records what it established, so a report can state it without a reader going to the source:\n\n@testset \"inflation preserves the tiling\" begin\n    err = norm(inflate(t) - reference)\n    evidence!(; tolerance = 1e-12, achieved = err, oracle = \"closed-form inflation matrix\")\n    @test err < 1e-12\nend\n\nEvidence attaches to the @testset currently running and travels with it into the record. Values that are not Real, Bool or AbstractString are stored as their string() form, so anything is safe to pass.\n\nOutside a @shard block evidence! does nothing, which keeps a test file runnable on its own.","title":"Attaching evidence"},{"category":"section","location":"records/#Where-they-go","page":"Records","text":"Set TESTSHARDS_OUT to a directory and the records are written there. Locally, with it unset, nothing is written — running the tests leaves no artefacts behind.\n\nUnder Pkg.test the suite is copied into a sandbox, so the directory must be an absolute path inside your workspace. The bundled workflow handles this.","title":"Where they go"},{"category":"section","location":"balancing/#Balancing","page":"Balancing","text":"","title":"Balancing"},{"category":"section","location":"balancing/#How-the-split-is-decided","page":"Balancing","text":"Each shard reads the same timing history and computes the same assignment: heaviest unit first into the least-loaded shard — longest-processing-time bin packing [1]. Because every shard runs the identical computation over the identical input, no coordination is needed — and a unit cannot land in two shards or in none.\n\nA unit with no history yet is assigned as it is encountered, spread evenly across shards. So a test added since the last recorded run is still guaranteed to run exactly once, with no planning step in between.\n\nWith no history at all — a fresh repository — the split is even but blind. It balances itself from the second push to the default branch, once timings have been recorded.","title":"How the split is decided"},{"category":"section","location":"balancing/#The-history","page":"Balancing","text":"Each shard writes the time each of its units took; CI merges them and stores the result on a ci-timings orphan branch. Only pushes to the default branch record: a pull request must never rewrite the history it was planned against.","title":"The history"},{"category":"section","location":"balancing/#How-many-shards-can-this-suite-use?","page":"Balancing","text":"A shard pays a fixed cost before running anything — checkout, depot restore, precompilation — and that cost does not shrink when you add shards:\n\nwall(N) = fixed + (load of the heaviest shard at N)\n\nThe second term falls as N grows, until the heaviest single unit dominates. From there, more shards change nothing and each still costs fixed.\n\nTestShards.diagnose finds that point:\n\njulia> using TestShards\n\njulia> t = TestShards.load_timings(\"timings.tsv\");\n\njulia> w = TestShards.load_shards(\"shards.tsv\");\n\njulia> TestShards.diagnose(t; n = 8, shards = w)\nTestShards diagnosis — 12 units\n  serial total      152.1s\n  fixed per shard   48.7s\n  predicted at N=8  95.5s wall, 542.0s runner\n  knee              N=4  (N=8 costs more for no gain)\n  floor             core/test_partition_large.jl  46.8s — no split finishes sooner than this\n  observed          201.0s wall, 542.0s runner over 8 shards\n  effective         0.8x of 8 — start window 130.0s (s1 first, s4 last)\n\n  heaviest units\n    46.8    core/test_partition_large.jl\n    34.4    core/test_unit_atomicity.jl\n    25.1    core/test_partition_small.jl\n\nknee — the smallest shard count that reaches the best wall clock. Past it you are paying for jobs that do not make the run finish sooner.\nfloor — the heaviest single unit. No split across jobs beats it, so lowering it is the only way to go faster.\n\nTo lower the floor, split that unit. The diagnosis names the heaviest @testsets inside it, so you know where to cut rather than guessing.","title":"How many shards can this suite use?"},{"category":"section","location":"balancing/#Did-the-shards-actually-run-at-the-same-time?","page":"Balancing","text":"Everything above assumes they did. A shard count only buys parallelism if the runners are available; when the queue is congested the run finishes at\n\n(when the LAST shard starts) + (its share of the work)\n\nand no amount of balancing changes that. So each shard also records when it ran, not only for how long, and the diagnosis reports the two numbers that make the difference visible:\n\nstart window — how long after the first shard the last one started.\neffective parallelism — the work divided by the wall clock it took. This is what the shard count was actually worth.\n\nThe run above is this package's own: 8 shards, and an effective parallelism of 0.8. The split was fine — the model says 95.5 s — but the last shard started 130 s after the first, so the run took 201 s. A run whose observed wall clock is far above the prediction says so explicitly in the CI summary, because the fix is a different one: fewer shards, or a runner pool that can actually start them together, not a better balance.\n\nEffective parallelism below 1 does not quite mean \"one job would have been faster\", though it is a strong hint. The per-unit seconds are measured in separate processes, each re-paying first-use compilation, so their sum overstates what a single job would take. Compare against a real unsharded run before concluding.\n\nStart times come from each runner's own clock. They are NTP-synced, so treat a spread of a second or two as noise rather than as a queue effect.","title":"Did the shards actually run at the same time?"},{"category":"section","location":"balancing/#Measuring-fixed","page":"Balancing","text":"Take one shard's job wall clock and subtract the time its units took; the remainder is the fixed cost. The shards report this themselves — the window each one recorded is exactly that subtraction — so diagnose measures fixed rather than guessing it, and the fixed-cost-seconds workflow input can stay at 0. Set it only to ask what a hypothetical price would do to the curve; an explicit value wins over the measured one.\n\nA shard can only see as far as its own test process, and its job keeps going afterwards — processing coverage, uploading artefacts. CI therefore stamps the job's end separately and the diagnosis folds it back in, because that tail is not small: measured here it is about half the fixed cost on this suite, and about a tenth of it on a large one.\n\nWithout that stamp the measured fixed is a lower bound, and so is every conclusion drawn from it — including FixedCostBound, which is the test that decides whether work stealing is worth anything. Understating the fixed cost biases that test towards saying no.\n\nfixed scales the reported cost but not the knee, so an unmeasured run still answers \"how many shards can this suite use\".","title":"Measuring fixed"},{"category":"section","location":"balancing/#What-is-actually-limiting-this-suite","page":"Balancing","text":"The numbers above support four different conclusions, and they want opposite actions. Rather than leave the reader to pick, the diagnosis decides — TestShards.bottleneck returns one of four types and TestShards.remedy and TestShards.usable_shards dispatch on it:\n\nregime what it means what to do\nQueueBound the shards never overlapped fewer shards, or a pool that can start them\nFixedCostBound a shard spends longer getting ready than testing lower the setup, or split less\nFloorBound the heaviest single unit is what is left cut that unit in two\nWorkBound nothing is in the way more shards would still help\n\nThey are tested in that order, because fixing one is what exposes the next: a queue-bound run says nothing about its own balance, since the balance was never given a chance to matter.\n\nThis is a type rather than a paragraph because the same numbers mean opposite things at different scales. A 49 s per-shard cost and a 130 s start window are fatal to a 150 s suite and a rounding error on a 40-minute one. Deciding which case you are in is a fact about your repository, and every consumer working it out again by hand is how the wrong one gets acted on.","title":"What is actually limiting this suite"},{"category":"section","location":"balancing/#Sharing-the-depot-cache","page":"Balancing","text":"All shards instantiate the same project, so they should share one dependency cache, and the bundled workflow does this: one cache-name for the whole matrix, include-matrix: false. A shard that starts after another has finished then restores that shard's depot instead of building its own, and only one copy is stored per run instead of one per shard.\n\nHow much that is worth depends entirely on the dependency tree, and the two ends look nothing alike. Measured on this package — one dependency — precompilation is 3–4 s of a ~49 s fixed cost, so the cache is nearly free and nearly pointless; what is left is installing Julia, processing coverage and starting a Pkg.test sandbox, none of which a depot cache touches. On a suite with a real dependency tree it is the other way round: precompilation dominates the fixed cost, and sharing the cache is the difference between paying it once and paying it once per shard.\n\nWhich of the two you are in is a fact about your repository, not about sharding, and it is exactly what the measured fixed above tells you. Read it before deciding anything costs too much.","title":"Sharing the depot cache"},{"category":"section","location":"#TestShards.jl","page":"Home","text":"Run a Julia test suite across several CI jobs at once, balanced by how long each part actually took last time.\n\nThe shardable units are whatever runtests.jl includes — literal or computed. There is no naming convention to follow and no manifest to keep in sync, and a bare Pkg.test() is unchanged: with no environment set, everything runs, in order.\n\nusing MyPackage, TestShards\n\nTestShards.@shard begin\n    include(\"core/a.jl\")\n    for f in readdir(joinpath(@__DIR__, \"solver\"); join = true)\n        include(f)\n    end\nend\n\njobs:\n  test:\n    permissions:\n      contents: write\n    uses: QAtlasHub/TestShards.jl/.github/workflows/sharded-tests.yml@main\n    with:\n      shards: 8\n    secrets: inherit","title":"TestShards.jl"},{"category":"section","location":"#Where-to-go","page":"Home","text":"Getting started — installing, wiring CI, running one shard locally.\nUnits — what gets split, keeping order-dependent files together, ordering.\nBalancing — the timing history, how many shards a suite can use, the diagnosis.\nRecords — what a run reports, and attaching evidence to a testset.\nGuarantees — what cannot silently go wrong, and the one rule your suite must follow.\nAPI — every exported and public function.","title":"Where to go"},{"category":"section","location":"#Scope","page":"Home","text":"This splits a suite across CI jobs. It does not run tests in parallel within a job — that is ParallelTestRunner.jl and ReTestItems.jl's business, and the two compose.","title":"Scope"}]
}
